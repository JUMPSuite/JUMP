# the following modules should be loaded before running
module load jump/JUMPg/alpha # for rundtas

module load jump/1.13.1 # for -s and -f (also dependencies of perl and R modules)
module swap jump/JUMPd/1.13.1 jump/JUMPd/alpha # for special version of -d



