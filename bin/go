#!/bin/bash
#
this_scripts_dir="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
#dtn=`date +%Y%m%d.%N`
pwd="$(pwd)"
#

#
echo "start (go)" $this_scripts_dir
echo "pwd" $pwd
echo "submitting... "
bsub < $this_scripts_dir/steps.sh
echo "done (go)"
#

